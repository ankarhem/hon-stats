@page "/heroes"
@model HeroesModel
@{
    ViewData["Title"] = "Heroes";
}

<h1>Heroes</h1>

<form class="hero-filter"
      hx-get="/heroes"
      hx-target="#hero-grid"
      hx-swap="innerHTML"
      hx-trigger="input changed delay:80ms, change">
    <input class="search__input hero-filter__search"
           type="search"
           name="q"
           value="@Model.Query"
           placeholder="Search hero name…"
           autocomplete="off"
           aria-label="Hero name" />
    <div class="hero-filter__roles" role="group" aria-label="Filter by role">
        @foreach (var (role, label) in HeroesModel.Roles)
        {
            <label class="role-toggle">
                <input type="checkbox" name="roles" value="@role"
                       checked="@(Model.ActiveRoles.Contains(role))" />
                <span>@label</span>
            </label>
        }
    </div>
</form>

<div id="hero-grid">
    <partial name="_HeroGrid" model="Model" />
</div>
